
@page "/vanir"
@using System.Reflection.Metadata
@using MudBlazor
@using NorseMythologyIndex.Models
@using NorseMythologyIndex.Services
@inject MythologyService MythologyService
@inject Microsoft.AspNetCore.Components.NavigationManager navigationManager

<PageTitle>Vanir</PageTitle>
<MudMainContent Style="overflow-y: auto; height:100vh; width: 100%" class="page-container">
    <h1 style="margin-bottom: 0.1rem;" >Vanir</h1>

    @if (isLoading)
    {
        <div class="loading">
            <p>Loading...</p>
        </div>
    }
    else if (vanirItems.Any())
    {
        @foreach (var group in MythologyService.GetGroupedItems(vanirItems))
        {
            <MudText Typo="Typo.h6">@group.Key</MudText>
            <MudDivider Style="margin-bottom: 0.7rem; border-color: black" ></MudDivider>
            <MudStack Style="margin-bottom: 1rem" Row="true" Wrap="Wrap.Wrap" Spacing="2">
                @foreach (var item in group)
                {
                    <tr style="width: auto;">
                        <td>
                            <NavLink class="item" href="@($"/item/{item.Key}")">@item.Key</NavLink>
                        </td>
                    </tr>
                    <td style="margin: 0 1rem; width: auto;">
                        ·
                    </td>
                }
            </MudStack>
        }
    }
    else
    {
        <p>No Æsir Items found.</p>
    }
</MudMainContent>

@code {
    private Dictionary<string, MythologyItem> vanirItems = new();
    private bool isLoading = true;
    private string? selectedItemName;
    
    
    protected override async Task OnInitializedAsync()
    {
        vanirItems = await MythologyService.GetItemsByTypeAsync("Vanir");
        isLoading = false;
    }
    
}

<style>
    .page-container {
        margin: 0 auto;
        padding: 6rem 30ch;
        height: fit-content;
    }

    h1 {
        color: #2c3e50;
        margin-bottom: 2rem;
        font-size: 2.5rem;
        text-align: center;
    }
    .tables-container{
        margin:auto;
        width: fit-content;
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        grid-gap: 5rem;
    }
    .as-table{
        height: fit-content;
    }
    .item {
        margin: 0;
        padding: 0;
        text-decoration: none;
        color: black;
    }
    .loading {
        text-align: center;
        padding: 3rem;
        font-size: 1.2rem;
        color: #666;
    }
</style>


